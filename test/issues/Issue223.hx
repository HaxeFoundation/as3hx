
class Issue223
{
    public function new()
    {
        var result : Float = Math.min(Math.min(Math.min(1, 2), 3), 4);
        var result : Float = Math.max(Math.max(Math.max(1, 2), 3), 4);
    }
}
