
class Issue70
{
    public function new(i : Int = 10, args : Array<Dynamic> = null)
    {
        trace(args);
    }
    
    private function test(args : Array<Dynamic> = null) : Void
    {
        trace(args);
    }
}
