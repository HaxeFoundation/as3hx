package {
	public class Vectors {
		private var _buffers:Vector.<BitmapData>;

		public static function blah() {
			_buffers = Vector.<BitmapData>([bottomBuffer]);
			var _intVec : Vector.<int> = [];
			var another : Vector.<String> = new Vector.<String>();
			var myVector:Vector.<String> = new Vector.<String>(7); // myVector is created with 7 elements
			var myVector:Vector.<String> = new Vector.<String>(7, true); // myVector's length is fixed to 7 elements
			var v:Vector.<String> = Vector.<String>(["Hello", "World"]); // basically a constructor
			mMatrixStack = new <Matrix3D>[];
			mStringVec = new <String>["hi","there"];
		}
	}
}
